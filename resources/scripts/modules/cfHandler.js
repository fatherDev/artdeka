/* const loader = $(this.container).find('.ajax-loader');
if (!loader.length) {
  $('div.wpcf7 > form').each(function () {
    $(this).wpcf7InitForm();
  });
} */

/* var cf_selector = 'div.wpcf7 > form';
var cf_forms = $(next.container).find(cf_selector);

if (cf_forms.length > 0) {
  $(cf_selector).each(function () {
    var $form = $(this);
    wpcf7.init($form[0]);
  });
} */

/* const ajaxLoader = document.querySelector('.ajax-loader');
const contactForm = document.querySelectorAll('div.wpcf7 > form');

if (!ajaxLoader) {
  const forms = document.querySelectorAll('.wpcf7 > form');
  forms.forEach((e) => wpcf7.init(e));
}
 */
