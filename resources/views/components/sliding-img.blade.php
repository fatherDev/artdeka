<div data-scroll data-scroll-speed="{{ $speed ?? '' }}" class="c-sliding-img__wrapper {{ $class ?? '' }}">
    <figure class="c-sliding-img">
        <picture>
            <img src="{{ $ACF ?? '' }}" alt="">
        </picture>
    </figure>
</div>
